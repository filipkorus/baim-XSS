import sanitizeHtml from 'sanitize-html';

const sanitizeMessage = (message: string): string => {
	const sanitizedMessage = message;

	// add message sanitization HERE

	return sanitizedMessage;
}

export default sanitizeMessage;
